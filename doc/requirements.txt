sphinx>=2.0  # BSD
openstack-doc-tools>=1.8.0 # Apache-2.0
os-service-types
# os-service-types uses typing extensions, but doesn't declare it
# as a dependancy.  While that is debugged, just include typing_extensions
# manually
typing_extensions
